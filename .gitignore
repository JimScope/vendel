.vscode
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.env
pb_data/
frontend/dist/
backend/ender
backend/vendel
backend/pb_public/
backend/ui/dist/*
!backend/ui/dist/.gitkeep

# Test data (auto-generated by go test)
**/test_pb_data/

# Modem agent binary (incl. cross-compiled .exe and other suffixes)
modem-agent/vendel-modem-agent*

# Modem agent runtime config — contains plaintext device API keys
modem-agent/modems.yaml

# Local agent / review tooling state (not part of the project)
.claude/scheduled_tasks.lock
.codex/

# Documento de auditoría/mejoras: local-only, no versionado
AUDITORIA.md

# Worktrees temporales de subagentes
.claude/worktrees/
